"""
predict.py - OncoRisk ML

Handles predictions on new/unseen data using the trained model.
Takes raw input, applies the same preprocessing (scaling), and
returns the predicted risk level.
"""

# Implementation will be added in Phase 8.
# Functions planned:
#   - predict_risk(input_data, model, scaler)
#   - format_prediction(prediction)
