"""
eda.py - OncoRisk ML

Exploratory Data Analysis module. Generates statistical summaries and
visualizations to understand data distributions, correlations, and
class balance before model training.

Why EDA matters:
    Before building models, you need to understand what the data looks like.
    Which features correlate with the target? Are there outliers? How
    imbalanced are the classes? These insights directly inform preprocessing
    and model choices.
"""

# Implementation will be added in Phase 3.
# Functions planned:
#   - plot_class_distribution(df, target_col)
#   - plot_correlation_matrix(df)
#   - plot_feature_distributions(df)
#   - generate_summary_stats(df)
